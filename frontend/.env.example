# Use VITE_API_BASE_URL when the browser should call the API directly.
VITE_API_BASE_URL=http://127.0.0.1:8000

# Use VITE_PROXY_TARGET when Vite should proxy /api requests during local dev.
VITE_PROXY_TARGET=http://127.0.0.1:8000
