# Cloudflare Pages

- Project root: `frontend`
- Build command: `npm run build`
- Output directory: `dist`
- Add `VITE_API_BASE_URL` in the Pages project settings so the frontend can call the deployed FastAPI backend.
